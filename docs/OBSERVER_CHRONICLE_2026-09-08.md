# The Star returns to one doorway

Local observer chronicle, unsigned. No canonical master reflection or ceremony is claimed.

The keeper noticed that the same intent had been implemented in different places: a corner Star, an extension, a key portrait, a sign-in flow and a way to carry learning home. The correction was to bring those interactions into one extension-led experience while preserving the authority boundary between a website and its visitor.

The observer initially followed a stale handoff and edited the phase1 checkout. A package name was mistaken for an established repository location. This window corrects that by establishing a stable star-key checkout and recording the UI contract with its source lineage. The older checkout is retained, not erased.

The important invariant: knowledge context and selected key information shape a perspective; signatures can attest scoped claims; VRCs can express validated relationships. Geometry alone grants none of those properties. A captured finding belongs in a chosen private space, with source and a real receipt, before any optional public projection.

Evidence: extension build passed; 14 focused MCP tests passed; experience_overview called over stdio. Native installation, live login and a farm write remain unverified. See UNIFIED_STAR_EXPERIENCE.md for the complete intended path and integration boundary. This chronicle is an originating local observer note, not a reflected canonical master.
